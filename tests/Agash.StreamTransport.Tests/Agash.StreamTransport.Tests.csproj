<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net11.0</TargetFrameworks>
    <TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('Windows'))">$(TargetFrameworks);net11.0-windows10.0.19041.0</TargetFrameworks>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Agash.StreamTransport.Abstractions\Agash.StreamTransport.Abstractions.csproj" />
    <ProjectReference Include="..\..\src\Agash.StreamTransport\Agash.StreamTransport.csproj" />
    <ProjectReference Include="..\..\src\Agash.StreamTransport.Signaling\Agash.StreamTransport.Signaling.csproj" />
    <ProjectReference Include="..\..\src\Agash.StreamTransport.Stun\Agash.StreamTransport.Stun.csproj" />
  </ItemGroup>

  <ItemGroup Condition="$(TargetFramework.Contains('-windows'))">
    <PackageReference Include="Vortice.Direct3D11" />
  </ItemGroup>

  <!-- The Linux alpha compute shaders are no longer embedded here. They live in
       Agash.StreamTransport (Codecs/Shaders), and AlphaShaderCompilationTests reads them out of that
       assembly, so the test validates the SPIR-V that actually ships rather than a second copy that
       could drift. The .comp -> .spv compilation happens at build time via glslc in the library
       csproj; the test does not run a GLSL compiler, because loading shaderc into the test process
       crashes when the Mesa VAAPI driver is also loaded (both export SPIRV-Tools symbols that the
       linker interposes). -->

  <PropertyGroup Condition="$(TargetFramework.Contains('-windows'))">
    <DefineConstants>$(DefineConstants);WINDOWS_D3D11</DefineConstants>
  </PropertyGroup>
</Project>
